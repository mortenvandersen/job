name: Owl Portfolio Job Scrape

on:
  schedule:
    - cron: '0 8 * * 1'  # Every Monday at 8am UTC

  workflow_dispatch:
    inputs:
      mode:
        description: 'initial = establish baseline, diff = new jobs only'
        required: false
        type: choice
        options:
          - diff
          - initial
        default: diff
      company:
        description: 'Single company name to scrape (optional)'
        required: false
        default: ''

permissions:
  contents: write

jobs:
  scrape:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          ref: main

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: pip install firecrawl-py anthropic python-dotenv

      - name: Run Owl scraper
        env:
          FIRECRAWL_API_KEY: ${{ secrets.FIRECRAWL_API_KEY }}
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: |
          MODE="${{ github.event.inputs.mode || 'diff' }}"
          COMPANY="${{ github.event.inputs.company }}"

          ARGS=""
          if [ "$MODE" = "diff" ]; then
            ARGS="--diff"
          fi
          if [ -n "$COMPANY" ]; then
            ARGS="$ARGS --company \"$COMPANY\""
          fi

          eval python scripts/owl_scrape.py $ARGS

      - name: Aggregate snapshots into owl_jobs.json and embed in index.html
        run: python scripts/aggregate_owl_jobs.py

      - name: Commit updated snapshots
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add scrapes/owl_snapshots/
          git add scrapes/owl_jobs.json
          git add index.html
          git add scrapes/owl_new_jobs.json 2>/dev/null || true
          echo "--- git status ---"
          git status
          if git diff --staged --quiet; then
            echo "No changes to commit."
          else
            MODE="${{ github.event.inputs.mode || 'diff' }}"
            git commit -m "Owl scrape (${MODE}): $(date +%Y-%m-%d)"
            git push origin main
          fi
